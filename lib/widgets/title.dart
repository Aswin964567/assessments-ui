import 'package:flutter/material.dart';

class TitleShareInfo extends StatelessWidget {
  const TitleShareInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}